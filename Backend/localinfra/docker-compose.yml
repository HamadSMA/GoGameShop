# docker run -p 127.0.0.1:8080:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:26.6.1 start-dev

services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.6.1
    container_name: keycloak
    ports:
      - "8080:8080"
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
    command: ["start-dev"]
    volumes:
      - keycloak-data:/opt/keycloak/data
volumes:
  keycloak-data:

# Nginx is not used in this project, but I'm keeping this snippetfor reference
# services:
#   nginx:
#     image: nginx
#     container_name: nginx-test
#     ports:
#       - "8080:80"
#     volumes:
#       - nginx-data:/usr/share/nginx/html
#     environment:
#       - MY_VAR="Hello my guy" 
    
# volumes:
#   nginx-data: